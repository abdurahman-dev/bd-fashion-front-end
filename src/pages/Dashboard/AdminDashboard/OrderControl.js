import React from 'react';
import DashboardLayout from '../DashboardLayout';

const OrderControl = () => {
    return (
        <DashboardLayout>
            order control
        </DashboardLayout>
    );
};

export default OrderControl;